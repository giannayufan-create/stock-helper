// Flash order — removed from decision-support product.

import type { ContractInfo } from '../lib/types/contract';

export function FlashOrder(_props: { contract: ContractInfo }) {
    return null;
}
